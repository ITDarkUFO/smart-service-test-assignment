@{
    ViewData["Title"] = "UserListCategoryGet";
}

<h1>@ViewData["Title"]</h1>

@if (Model.Count == 0)
{
    <p>Ничего не найдено</p>
}

<table class="table table-striped">
    <thead>
        <tr>
            <th>UserId</th>
            <th>ListCategoryId</th>
        </tr>
    </thead>
    @foreach (var userlist in Model)
    {
        <tr>
            <td>@userlist.UserID</td>
            <td>@userlist.ListCategoryID</td>
        </tr>
    }
</table>